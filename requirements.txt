# Synthetic Data Forge — Python 3.11+
# Pin major versions; allow minor/patch upgrades.

# API
openai~=1.50              # OpenAI-compatible client (BYOK base_url + key)
fastapi~=0.115            # REST layer
uvicorn[standard]~=0.32   # ASGI server for FastAPI
httpx~=0.27               # async HTTP (openai SDK transport)

# Storage
lancedb~=0.13             # vector store + semantic dedup
duckdb~=1.1               # metadata analytics + coverage queries
pyarrow~=17.0             # required by lancedb + duckdb interop

# Pipeline
python-dotenv~=1.0        # .env loading in config.py
pydantic~=2.9             # typed records flowing through queues
# asyncio + asyncio.Queue come from the stdlib — no install needed

# Dedup
datasketch~=1.6           # MinHash LSH (Jaccard threshold 0.7)
vendi-score~=0.0.3        # diversity metric on accepted set
sentence-transformers~=3.2 # embeddings for LanceDB cosine sim
numpy~=1.26               # vector math; pinned <2 for compat with deps

# Dev
pytest~=8.3
pytest-asyncio~=0.24
ruff~=0.7
mypy~=1.13
black~=24.10
