"""Taxonomy builder — expands task spec + domain into a tree of seed topics."""
from __future__ import annotations


def build_taxonomy(task_spec: str, domain: str) -> list[dict]:
    raise NotImplementedError
